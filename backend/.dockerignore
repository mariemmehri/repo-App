# Le pipeline CI dépose le JAR dans target/ avant docker build.
# Ne pas ignorer target/*.jar sinon COPY target/*.jar échoue.

# Fichiers IDE et Git
.git
.vscode
.idea
*.log
.classpath
.project
.settings
.factorypath

# Ne pas copier le Dockerfile lui-même
Dockerfile
.dockerignore