# Ignorer les dépendances locales
node_modules
.angular

# Le pipeline CI dépose le build Angular dans dist/ avant docker build.
# Ne pas ignorer dist sinon COPY dist/... échoue.
out-tsc

# Fichiers IDE et Git
.git
.vscode
.idea
*.log

# Ne pas copier le Dockerfile lui-même dans l'image
Dockerfile
.dockerignore